import { Typography } from 'antd';

const { Title, Text } = Typography;

/** 儀表板佔位頁面，待後續實作 */
const DashboardPage = () => (
  <div>
    <Title level={4}>儀表板</Title>
    <Text type="secondary">功能開發中…</Text>
  </div>
);

export default DashboardPage;
